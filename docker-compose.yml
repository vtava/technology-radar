services:
  radar-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"

  radar-web-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
